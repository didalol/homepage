import { cn } from "@/lib/utils";
import Image from "next/image";

- 搭建 ES 集群，使用 Elasticsearch 实现搜索功能
- 搭建 Redis 集群，使用 Redis 实现缓存功能
- 使用 Nextjs 创建博客系统，使用 Prisma 实现数据库操作

export default ({ children }) => {
  return <>{children}</>;
};
